# Before running

Update the RPC URL in *src/index.tsx* line 12

```typescript
const endpoint = '<RPC URL>';
```

Install the packages

```shell
npm install
```

# How to run

```shell
npm run start
```